<!--logs.wxml-->
<view class="container log-list">
  <block wx:for="{{list}}" wx:for-item="item" wx:key="item.id">
    <view class="activity-item" bindtap="show" data-id="{{item.id}}" data-title="{{item.title}}">
      <text>{{item.title}}</text>
    </view>
  </block>
</view>