<view>
  <view class='userinfo'>
    <button size='mini' wx:if="{{!hasUserInfo}}" bindtap='login'>登录</button>
    <block wx:else>
      <image class='userinfo-avatar' src="{{userInfo.avatarUrl}}" />
      <text class='userinfo-nickname'>{{userInfo.nickName}}</text>
    </block>
  </view>
</view>